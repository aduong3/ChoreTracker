import {useState} from 'react';
import PropTypes from 'prop-types';

const Shop = ({onClose}) => {


return (
    <>
    <h2>Testing</h2>
    </>
);

};

Shop.propTypes = {
    onClose: PropTypes.func.isRequired,
};

export default Shop;